fmt_integral() must be modified to be able to print 64 bit values using only
32 bit integers.

Header files should only import other public header files, not private ones.

iarray_sort() needs more automated testing.
